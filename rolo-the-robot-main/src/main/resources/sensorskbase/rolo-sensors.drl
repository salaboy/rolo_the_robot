package com.salaboy.rolo;

import com.salaboy.rolo.model.*;
import com.salaboy.rolo.model.reports.*;

import com.salaboy.rolo.api.*;
import com.salaboy.rolo.api.Motor.*;



rule "test sensor"
    when
        DistanceReport( $d: distance ) over window:time( 300ms ) from entry-point "distance-sensor"
    then
        System.out.println(" - Distance: "+$d);
end











